options:
  rabbitmqctl:
    name: rabbitmqctl path
    notes: The full path to the rabbitmqctl script
    default: /usr/lib/erlang/lib/rabbitmq_server-1.5.0/sbin/rabbitmqctl
  queue:
    name: Queue
    notes: The name of the queue to collect detailed metrics for
