metadata:
  doc_count:
    label: Documents
    units: documents
    precision: "0"
  doc_del_count:
    label: Deleted documents
    units: documents
    precision: "0"
  disk_size:
    label: Index size
    units: bytes
