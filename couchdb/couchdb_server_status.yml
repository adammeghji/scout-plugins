metadata:
  version:
    label: CouchDB Version
  request_time_mean:
    label: Mean request time
    units: milliseconds
  request_time_max:
    label: Max request time
    units: milliseconds
  request_time_min:
    label: Min request time
    units: milliseconds
  request_time_stddev:
    label: Request time standard deviation
  database_reads_sum:
    label: Database read count
    precision: "0"
  database_reads_mean:
    label: Mean database read count/second
    units: /sec
    precision: "1"
  database_reads_max:
    label: Max database read count/second
    units: /sec
    precision: "1"
  database_reads_stddev:
    label: Database read count standard deviation
  database_writes_sum:
    label: Database write count
    precision: "0"
  database_writes_mean:
    label: Mean database write count/second
    units: /sec
    precision: "1"
  database_writes_max:
    label: Max database write count/second
    units: /sec
    precision: "1"
  database_writes_stddev:
    label: Database write count standard deviation

