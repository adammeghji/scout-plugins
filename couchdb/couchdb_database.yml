metadata:
  doc_count:
    label: Documents
    units: documents
  doc_del_count:
    label: Deleted documents
    units: documents
  disk_size:
    label: Database size
    units: bytes
  purge_seq:
    label: Purge operations
    units: purge operations
  update_seq:
    label: Update operations
    units: updates
